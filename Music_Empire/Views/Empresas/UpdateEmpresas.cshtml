@model Music_Empire.Models.Empresas
@{
    ViewBag.Title = "UpdateEmpresas";
}

<h2>UpdateEmpresas</h2>




<div class="panel">
    <form action="~/Empresas/UpdateEmpresas" method="post">



        <div class="form-group">

            <input class="form-control" type="hidden" id="idEmpresa" name="idEmpresa"  value="@Model.idEmpresa" />
        </div>

        <div class="form-group">
            <label>Nome</label>
            <input class="form-control" placeholder="Nome da Empresa" type="text" id="nomeEmpresa" name="nomeEmpresa" value="@Model.nomeEmpresa" />
        </div>

        <div class="form-group">
            <label>Endereço</label>
            <input class="form-control" placeholder="Endereço da Empresa" type="text" id="enderecoEmpresa" name="enderecoEmpresa" value="@Model.enderecoEmpresa" />
        </div>

        @*<div class="form-group">
            <label>Estado</label>
            <select class="form-control" id="SelectedUserRoleId" name="localEmpresa.idLocal">
                <option value="value">Selecione um Estado</option>

                @foreach ( Music_Empire.Models.Local item in @ViewBag.ListEstado)
                {
                    <option value="@item.idLocal">@item.nomeEstado</option>
                }

            </select>
        </div>*@

       <div class="form-group">
            <label>Cidade</label>
            <select class="form-control" id="SelectedUserRoleId" name="localEmpresa.idLocal">
                <option value="@Model.localEmpresa.idLocal">@Model.localEmpresa.nomeCidade</option>

                @foreach (Music_Empire.Models.Local item in @ViewBag.ListEstado)
                {
                    <option value="@item.idLocal">@item.nomeCidade</option>
                }

            </select>
        </div>



        <button class="btn btn-default" type="submit">Salvar</button>
    </form>
</div>
